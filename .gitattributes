# Auto-detect text files and normalize line endings
* text=auto

# Enforce LF for source files (preferred for cross-platform repos)
*.java    text eol=lf
*.js      text eol=lf
*.html    text eol=lf
*.css     text eol=lf
*.xml     text eol=lf
*.py      text eol=lf
*.sh      text eol=lf
*.md      text eol=lf

# Keep Windows batch files with CRLF for compatibility
*.bat     text eol=crlf

# Binary files — do not attempt to normalize
*.png     -text
*.jpg     -text
*.jpeg    -text
*.gif     -text
*.zip     -text
*.jar     -text
